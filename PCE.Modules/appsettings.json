{
  "ConnectionStrings": {
    "EscapeManagement": "Host=localhost;Database=${ESCAPE_MANAGEMENT_DB};Username=${DB_USER};Password=${DB_PASSWORD}",
    "ItineraryManagement": "Host=localhost;Database=${ITINERARY_MANAGEMENT_DB};Username=${DB_USER};Password=${DB_PASSWORD}"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}